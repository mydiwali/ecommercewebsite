# Copy to .env.local (git-ignored) for local testing of `npm run deploy`.
# In CI, the same two values come from GitHub repo secrets instead.
HOSTINGER_API_TOKEN=your-hostinger-api-token-here
HOSTINGER_DOMAIN=mydiwalicrackers.com
